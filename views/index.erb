<!DOCTYPE html>
<html>
<head>
  <title>Restroom Status</title>
  <link rel="icon" type="image/x-icon" href="/favicon.ico">

  <style type="text/css">
    html, body, .container, .room {
      height: 100%;
      padding: 0px;
      margin: 0px;
    }

    .room {
      display: block;
      position: relative;
      float: left;
      width: 100%;
      /*width: 50%;*/
      text-align: center;
    }

 /*   .room:first-of-type {
      border-right: 5px white dashed;
      width: 100%;
      width: calc(50% - 5px);
    }
*/
    .room p {
      color: white;
      font-family: Futura, 'Trebuchet MS', Arial, sans-serif; 
      font-size: 3em;
      margin-top: 10%;
      /*margin-top: 40%;*/
      text-transform: uppercase;
      text-shadow: 0 0 30px rgba(244,206,49,0.6), 0 0 60px rgba(244,206,49,0.4), 0 0 100px rgba(244,206,49,0.2), 0 0 90px rgba(244,206,49,0.1); 
    }

    .busy {
      background-color: #C88590;
    }

    .free {
      background-color: #5AADA4;
    }
  </style>
</head>
<body>
  <div class="container">
    <% room = Room.find(3) %>

    <div class="room <%= room.status %>" data-room-id=<%= room.id %>>
      <p>
        <%= room.name %> &blk14; <%= room.status %>
      </p>
    </div>

  </div>
</body>
</html>

